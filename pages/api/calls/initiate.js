export default function handler(req, res) {
  // Initiate a call
  // TODO: initiate outbound call using selected agent and campaign
  return res.status(200).json({ message: 'Initiate call endpoint' });
}
