export default function handler(req, res) {
  // Handle user registration
  // TODO: register a new user and return authentication details
  return res.status(200).json({ message: 'Register endpoint' });
}
