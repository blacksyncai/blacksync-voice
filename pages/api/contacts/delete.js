export default function handler(req, res) {
  // Delete a contact
  // TODO: remove contact from database based on provided ID
  return res.status(200).json({ message: 'Delete contact endpoint' });
}
